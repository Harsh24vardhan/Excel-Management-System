A> Install Mysql and run following query in root user(password root);

  1.Create Database using below command 
      create database excel_reader;

  2. select database
      use excel_reader;
  3. Create TABLE

      CREATE TABLE students (
        id INT AUTO_INCREMENT,
        name varchar(64),
          phone_number varchar(10) ,
          roll_no INT ,
          father_name VARCHAR(64),
          mother_name VARCHAR(64),
          standard INT,
          section char,
          primary key (id)
      );

B> Now Run following command in terminal;
  1. npm install
  2. npm start




